<table class='table'>
  <thead>
    <tr>
      <th></th>
      <th>name</th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= link_to user_path(books.user) do %>
      <td><%= attachment_image_tag @user, :profile_image_id, :fill, 100, 100, format: 'jpeg', fallback: 'no_image.jpg', size:'50x50', class: "attachment user profile_image fallback" %><br>
          <%= @user.name %></td>
      <td><%= books.title %></td>
      <td><%= books.body %></td>
      <td><%= link_to 'Edit', edit_book_path(books) %></td>
    </tr>
  <% end %>
  </tbody>
</table>